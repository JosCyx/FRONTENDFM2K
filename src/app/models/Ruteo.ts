export class Ruteo{
    tipoSol: number;
    area: number;
    nivel: number;

    constructor(tipoSol: number, area: number, nivel: number){
        this.tipoSol = tipoSol;
        this.area = area;
        this.nivel = nivel;
    }
}