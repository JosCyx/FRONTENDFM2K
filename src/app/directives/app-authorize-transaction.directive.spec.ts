import { AppAuthorizeTransactionDirective } from './app-authorize-transaction.directive';

describe('AppAuthorizeTransactionDirective', () => {
  it('should create an instance', () => {
    const directive = new AppAuthorizeTransactionDirective();
    expect(directive).toBeTruthy();
  });
});
